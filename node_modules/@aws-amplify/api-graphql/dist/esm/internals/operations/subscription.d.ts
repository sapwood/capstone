import { ModelIntrospectionSchema, SchemaModel } from '@aws-amplify/core/internals/utils';
import { ModelOperation } from '../APIClient';
export declare function subscriptionFactory(client: any, modelIntrospection: ModelIntrospectionSchema, model: SchemaModel, operation: ModelOperation): (args?: any) => import("rxjs").Observable<any>;
